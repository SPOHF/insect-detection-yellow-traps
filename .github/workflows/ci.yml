name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  quality-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20.11.1"
          cache: "npm"
          cache-dependency-path: apps/frontend/package-lock.json

      - name: Backend compile checks
        run: python -m compileall apps/backend/app src

      - name: Frontend build checks
        working-directory: apps/frontend
        run: |
          npm ci
          npm run build

      - name: Build backend container
        run: docker build -t swd-monitoring-backend:ci apps/backend

      - name: Build frontend container
        run: docker build -t swd-monitoring-frontend:ci apps/frontend
